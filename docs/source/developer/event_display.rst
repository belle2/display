Event Display
=============

Currently, the mdst data is stored in the .root file. To utilize the Phoenix framework and display the event on the browser, it is required to convert the .root file to .json file. To do that task, JSRoot is used.
